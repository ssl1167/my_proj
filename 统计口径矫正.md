通过对比你提供的项目代码（特别是 `garl_sabre/qiskit_runner.py` 和 `evaluate.py`）以及那份关于门数量统计综述的文献，我发现了你的代码在评估指标和统计口径上确实存在几个与学术界严谨标准脱节的“陷阱”。

总体来说，你的代码在 SWAP 分解为 CNOT 的换算上是正确的（代码中明确写了 `3.0 * additional_swap_count`，这与综述中“$1 \text{ SWAP} = 3 \text{ CNOTs}$”的基石法则完全吻合 ）。但是，**在“总门数（Total Gate Count）”的基础统计和比对机制上存在严重误差**，这极大概率导致了你 `output` 文件夹中实验结果显得异常或次优。

以下是具体的统计口径问题及代码对应位置：

### 1. 致命缺陷：未隔离“零成本”的虚拟门与非物理指令（Total Gate Count Trap）

**问题出在 `garl_sabre/qiskit_runner.py` 中的 `circuit_gate_profile` 函数：**

```python
def circuit_gate_profile(circuit: QuantumCircuit) -> Dict[str, float]:
    total_gates = float(len(circuit.data)) # <-- 这里的统计极为粗暴
    twoq_gates = float(count_two_qubit_gates(circuit))
    oneq_gates = max(0.0, total_gates - twoq_gates)
    ...

```

* 
**综述诊断：** 综述在第3.3节明确指出，在现代超导量子硬件中，像绕 Z 轴的单比特旋转门（$R_z$）属于“虚拟门（Virtual Gates）”，它们通过经典控制软件中的数字帧跟踪瞬间实现，物理执行时间接近于0，且硬件物理错误率为0 。此外，`len(circuit.data)` 还会把 Qiskit 中的 `barrier`（屏障指令）、`measure`（测量指令）全部当成等价的物理门计入总数。


* 
**实验影响：** 综述总结提到，如果在统计中包含具备完全零物理错误率的编译重组虚拟门，这种“总门数”统计体系具有极强的欺骗性与极端的误导性 。你的代码直接用 `len(circuit.data)` 计算 `gate_count_all`，导致生成的 CSV 报告中 `original_gate_count_all` 和 `additional_gates_total` 严重虚高，掩盖了 RL 算法真正在纠缠门上的优化效果。



### 2. 单比特门优化带来的负面干扰（附加门总数的计算逻辑）

**问题出在 `garl_sabre/qiskit_runner.py` 中的 `_build_metrics` 函数：**

```python
additional_gates_total = float(routed_profile["gate_count_all"] - original_profile["gate_count_all"])
additional_1q_total = float(routed_profile["oneq_count_all"] - original_profile["oneq_count_all"])

```

* 
**综述诊断：** 综述第4.1节指出，大量前沿 RL 论文（包括 Quarl 和 Amer 等人的研究）果断放弃了直观宏观的“总门数”，转而将统计中心聚焦于“双量子比特门数”，因为 1 个 CNOT 门的错误率等效于 10 到 100 个单量子比特门 。


* **实验影响：** 在 Qiskit 的转译过程（Transpilation）中，即使是 Sabre 路由，也有可能在插入 SWAP 的同时，触发了一些局部的单比特门合并或消除法则（特别是在 `optimization_level > 0` 时）。这就导致 `additional_1q_total` 可能是一个无法预测的负数或奇怪的数值。你的 `evaluate.py` 最终在生成 `paper_csv` 时输出了 `additional_gates_total`，这个指标混合了单双比特门的增减，引入了巨大的噪声。

### 3. 基准对齐问题（纯路由门开销不严谨）

**问题出在评价表（`evaluate.py` 的 CSV 导出项）：**

```python
paper_df = df[
    [
        "family", "name", "initial_mapper", "mode", "swap_count",
        "original_gate_count_all", "additional_gates_total", # <-- 这里输出容易引发质疑
        "additional_swap_count", "routing_time_sec", "evaluating_router",
    ]
].copy()

```

* 
**综述诊断：** 学术界评价纯路由开销（Routing Overhead），通常是指必须满足硬件拓扑约束而经过路由映射后的最终物理电路与初始逻辑电路之间的纯差值 。为了保证置信度，评估体系必须将统计红线严格孤立于“双量子比特门数（CNOT Gate Count）”与“新增SWAP动作开销”这两个核心高敏指标上 。



---

### 建议修复方案

为了让你的实验结果与顶级会议论文（如 AlphaRouter, Quarl）的统计口径对齐，你需要对代码进行以下修改：

**第一步：重构门计数逻辑（过滤非物理门与零成本门）**
修改 `garl_sabre/qiskit_runner.py` 中的 `circuit_gate_profile`，剔除 `barrier`、`measure` 以及 $R_z$：

```python
def circuit_gate_profile(circuit: QuantumCircuit) -> Dict[str, float]:
    # 剔除指令类和虚拟门
    physical_ops = [
        item for item in circuit.data 
        if getattr(item, "operation", item[0]).name not in ["barrier", "measure", "rz", "delay"]
    ]
    
    total_physical_gates = float(len(physical_ops))
    twoq_gates = float(count_two_qubit_gates(circuit))
    oneq_gates = max(0.0, total_physical_gates - twoq_gates)
    
    # ... 后续代码保持不变 ...

```

**第二步：在论文报告中聚焦 CNOT/SWAP**
修改 `evaluate.py` 中导出 `paper_csv` 的列，不要再展示混合了单双比特门的 `additional_gates_total`，而是展示真正的物理硬通货：

```python
paper_df = df[
    [
        "family",
        "name",
        "initial_mapper",
        "mode",
        "swap_count",
        "original_cnot_count_all",      # 替换 original_gate_count_all
        "routed_cnot_equiv_count",      # 加入路由后的等效CNOT总数
        "additional_cnot_equiv_from_swap", # 替换 additional_gates_total
        "routing_time_sec",
        "evaluating_router",
    ]
].copy()

```

经过这样调整后，你的实验数据（尤其是基准对比）将不会被无用的单比特门或虚拟指令掩盖，能够准确反映 RL 在减少由于拓扑受挫带来的 CNOT（纠缠门）开销上的真实性能。

经过对你新提供的文件（`evaluate_paper19.py`、`run_sabre_baseline.py`、`garl_sabre/env.py` 和 `garl_sabre/config.py`）的仔细审查，我发现**你的强化学习训练逻辑本身是安全的，但你所有的评估和基准测试脚本都深度“感染”了上一次提到的“总门数（Total Gate Count）统计陷阱”。**

以下是具体的问题分布以及修改建议：

### 1. 训练环境是安全的（好消息）

在 `garl_sabre/config.py` 和 `garl_sabre/env.py` 中，你的 RL 奖励配置 (`RewardConfig`) 默认将 `alpha_swap` 设为 1.0，而其他门级惩罚为 0.0。在 `env.py` 的终点奖励计算中，代理的核心惩罚是基于 `terminal_objective`（即 `swap_count`）。
**结论：** 你的 RL 代理在训练时确实是在优化物理硬通货（SWAP/CNOT），**它的学习方向是正确的，并没有被虚拟门误导**。

### 2. `evaluate_paper19.py` 中的统计展示偏差

这个文件专门用于生成对比表格（Comparison Table）和论文用表，它是最终实验结果的“出口”。但是它严重依赖了之前存在缺陷的总门数指标。

**问题代码位置：**
在 `generate_comparison_table` 函数和文件末尾导出 CSV 的地方，你强行把混合了虚拟门的总门数作为核心对比指标：

```python
    value_cols = [
        "swap_count",
        "original_gate_count_all",  # ❌ 包含了零物理成本的 Rz 门等
        "additional_gates_total",   # ❌ 混合了单比特门的噪声指标
        "additional_swap_count",
        "routing_time_sec",
        "routed_cnot_equiv_count",  # ✅ 这个才是真正有物理意义的指标
        "routed_gate_count_all",    # ❌ 
        "depth_overhead",
    ]

```

**修改建议：**
你需要彻底将报告的焦点从“Total Gates”转移到“CNOTs”。
将 `value_cols` 以及输出到 `paper_table.csv` 的列修改为：

```python
    value_cols = [
        "swap_count",
        "original_cnot_count_all",       # 替换原有的 original_gate_count_all
        "routed_cnot_equiv_count",       # 路由后的等效 CNOT 总数（关键指标！）
        "routed_cnot_raw_count",         # 路由后的纯 CNOT 数量
        "additional_cnot_equiv_from_swap", # 替换 additional_gates_total
        "additional_swap_count",
        "routing_time_sec",
        "depth_overhead",
    ]

```

并在末尾的 `df[[...]].to_csv(paper_table_path, index=False)` 中做同样的替换。否则，审稿人或使用者会看到 RL 算法增加了很多 `additional_gates_total`，而不知道那其实大都是 Qiskit 在编译过程中重组出的零成本 `rz` 门。

### 3. `run_sabre_baseline.py` 的基准对比陷阱

这是一个独立的基准测试脚本，用于跑 Qiskit SABRE 基线，但它同样陷入了单双比特门不分的统计泥潭。

**问题代码位置：**
在这个脚本最后，你通过 Pandas 打印了各算法的平均性能对比汇总：

```python
    summary_cols = [
        "swap_count",
        "additional_gates_total",  # ❌ 会显示 SABRE 增加了极少的门，造成 RL 不如基线的假象
        "additional_swap_count",
        "routed_cnot_equiv_count",
        "routing_time_sec",
        "original_gate_count_all", # ❌
    ]
    print(df.groupby(["method", "family"])[summary_cols].mean(numeric_only=True))

```

**修改建议：**
将终端打印的 `summary_cols` 改为反映物理退相干成本的指标：

```python
    summary_cols = [
        "swap_count",
        "additional_cnot_equiv_from_swap", # 准确反映增加的物理纠缠成本
        "routed_cnot_equiv_count",         # 最终的物理 CNOT 开销
        "routing_time_sec",
        "original_cnot_count_all",         # 初始 CNOT 数量
    ]

```

### 总结

你的底层逻辑（`garl_sabre/qiskit_runner.py`）中计算的 `routed_cnot_equiv_count`（等效 CNOT 总数）和 `additional_cnot_equiv_from_swap` 是**完全符合前沿文献综述（如 Quarl, AlphaRouter）标准的优秀指标**。

你现在面临的唯一问题是：在所有对外报告结果的代码（`evaluate.py`, `evaluate_paper19.py`, `run_sabre_baseline.py`）中，**把错误的数据（`gate_count_all`）摆在了台面上，而把正确的数据（`cnot_equiv_count`）藏在了后面**。

只要把这些脚本里所有的 `*_gate_count_all` 和 `additional_gates_total` 替换或降级，把 `*_cnot_*` 相关的指标提升为核心对比维度，你的实验结果就能完美对应上学术界的严谨评价体系了。